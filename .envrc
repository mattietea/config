#!/usr/bin/env bash

# Increase timeout for cold cache Nix evaluation (~6s)
export DIRENV_WARN_TIMEOUT=30s

eval "$(devenv direnvrc)"

# Use devenv with caching. Cold cache takes ~6s, cached ~350ms.
# Add --quiet to suppress devenv task output after initial setup.
use devenv
